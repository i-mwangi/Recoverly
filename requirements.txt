# Python 3.11+ (required by Band). Verified with Python 3.14.6 on Windows.
python-dotenv>=1.0,<2
band-sdk==1.6.0
flask>=3.0,<4
httpx>=0.27,<1
resend>=2.44,<3
pypdf>=6,<7
holidays>=0.50,<1
tzdata>=2024.1

# Local verification tools. External service calls use httpx/Resend above.
pytest>=8,<10
pytest-asyncio>=1,<2
pytest-cov>=5,<8
ruff>=0.5,<1
